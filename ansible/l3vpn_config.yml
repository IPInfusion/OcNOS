---
  - hosts: ocnos
    gather_facts: no
    

    tasks:
      - name: create L3VPN config file
        template:
          src: "templates/{{ ansible_network_os }}_l3vpn.j2"
          dest: "configs/{{inventory_hostname}}.config"
     
      - name: load L3VPN config ocnos
        ocnos_config:
          src: "configs/{{ inventory_hostname }}.config"
          
      - pause: seconds=15
          
      - name: show BGP session ocnos    
        cli_command:
          command: show running-config          
        register: result
        
      - name: debug
        debug:
          msg: "{{ result.stdout_lines }}"
          
